//- Все примеси в этом файле должны начинаться c имени блока (_roomInfo)

mixin _roomInfo(roomNumber, roomPrice, _type, _size, mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +_roomInfo('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' _roomInfo--' + modsList[i].trim();
      }
    }
    if (_size == undefined) {
        _size = 1;
    } else {
      _size = '_roomInfo__value--size' + _size; 
    }

  ._roomInfo(class=allMods)&attributes(attributes)
    +_flexRow()
      ._roomInfo__numer
        |№ 
        if (_size != 1) 
          span._roomInfo__value(class=_size) #{roomNumber}
        else
          span._roomInfo__value #{roomNumber}
        
        span._roomInfo__luxe #{_type}
      ._roomInfo__price 
        span._roomInfo__priceValue #{roomPrice} 
        | в сутки


    ._roomInfo__inner
      block
