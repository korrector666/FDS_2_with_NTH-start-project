//- Все примеси в этом файле должны начинаться c имени блока (_rangeSlider)

mixin _rangeSlider(text, options, mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +_rangeSlider('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' _rangeSlider--' + modsList[i].trim();
      }
    }

  ._rangeSlider(class=allMods)&attributes(attributes)
    label._rangeSlider__info #{text}
    label._rangeSlider__info._rangeSlider__info--state #{options.state}  
    ._rangeSlider__inner
      block
