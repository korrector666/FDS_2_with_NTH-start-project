//- Все примеси в этом файле должны начинаться c имени блока (_login)

mixin _login(text, mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +_login('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' _login--' + modsList[i].trim();
      }
    }

  ._login(class=allMods)&attributes(attributes)
    +_title('Войти','main')
    +_textField({
        name:'email',
          placeholder:'Email',
          },'noMargin, pt0')
    +_textField({
          name:'pass',
          placeholder:'Password',
          },'pt10')
    +btn('войти', 'coloredBig')
    +_flexRow
      div Нет аккаунта на Toxin?
      +btn('Создать', 'bordered ,mt12, noMR')          
    ._login__inner
      block
